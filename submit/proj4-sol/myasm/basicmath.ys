# Execution begins at address 0 
        .pos    0
        
init:   irmovq stack, %rsp
        call main
        halt

main:   irmovq 1, %rcx
        addq %rcx, %rax
        subq %rcx, %rax
        ret

#stack starts here and grows to lower addresses  
        .pos   0x200
stack:
